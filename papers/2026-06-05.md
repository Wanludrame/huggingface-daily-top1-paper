# HuggingFace Daily Papers Top 1 - 2026-06-05

## ZipSplat: Fewer Gaussians, Better Splats

- **arXiv ID**: 2606.05102
- **作者**: Alexander Veicht, Sunghwan Hong, Dániel Baráth, Marc Pollefeys
- **提交者**: Sunghwan Hong (@hongsunghwan)
- **Upvotes**: 10
- **HuggingFace 链接**: https://huggingface.co/papers/2606.05102
- **arXiv 链接**: https://arxiv.org/abs/2606.05102

---

## 论文解读

### 一、核心贡献与创新点

1. **解耦高斯分布与像素网格**：现有前馈式3DGS方法每个输入像素预测一个高斯，导致表示预算与相机分辨率绑定而非场景复杂度。ZipSplat提出基于token的方案，让高斯放置独立于像素网格。

2. **基于聚类的自适应压缩**：通过k-means将稠密视觉token压缩为紧凑的场景token，使得同一训练模型在推理时可灵活调节质量-效率曲线，无需重新训练。

3. **显著的效率提升**：用约 $\frac{1}{6}$ 的高斯数量超越像素对齐方法，在无真值位姿条件下达到SOTA。

4. **强泛化能力**：在DL3DV和RealEstate10K上分别超越最佳pose-free基线2.1dB和1.2dB PSNR，且零样本泛化至Mip-NeRF360和ScanNet++。

### 二、技术方法分析

整体流程可概括为：

```mermaid
graph LR
    A[多视图输入] --> B[多视图Backbone提取稠密视觉Token]
    B --> C[K-Means聚类压缩为场景Token]
    C --> D[Cross-Attention + Self-Attention精炼]
    D --> E[轻量MLP解码为高斯组]
    E --> F[无约束3D位置的高斯渲染]
```

- **多视图Backbone**：提取每张图像的稠密特征表示
- **K-Means聚类**：关键创新——将大量像素级token压缩为少量场景级token，数量由场景复杂度决定而非分辨率
- **注意力精炼**：交叉注意力融合多视图信息，自注意力建模token间关系
- **MLP解码**：每个场景token解码为一组高斯，位置不受网格约束
- **无需位姿**：整个流程无需GT相机内外参

核心设计哲学：**平坦墙面和复杂纹理物体不应分配相同数量的高斯**。

### 三、潜在影响与应用场景

| 方向 | 说明 |
|------|------|
| 实时3D重建 | 6倍压缩使移动端/边缘设备部署成为可能 |
| 自动驾驶 | 无需精确位姿的场景重建降低系统依赖 |
| AR/VR内容生成 | 高效表示加速流式渲染 |
| 数字孪生 | 零样本泛化能力支持多场景快速建模 |
| 3DGS压缩研究 | 为后续自适应表示预算分配提供新范式 |

该工作的"推理时可调"特性尤其重要，使得单一模型适配从低算力到高质量的多种部署需求。

### 四、推荐理由

1. **问题定义精准**：直指当前像素对齐方法的根本缺陷——资源分配与场景复杂度脱节
2. **方案优雅简洁**：k-means聚类作为桥梁，既简单又有效，且推理时灵活可控
3. **实验充分有力**：多数据集SOTA + 零样本泛化 + 大幅效率提升的三重验证
4. **实用价值高**：无需位姿、无需重训、可调压缩率，工程部署友好
5. **启发性强**：token化思想为3D表示学习中"按需分配"开辟新方向

---

**一句话总结**：ZipSplat通过将像素级高斯预测转化为基于聚类的场景token解码，以 $\sim 6\times$ 更少的高斯实现更优质量，优雅地解决了3DGS中表示预算与场景复杂度不匹配的核心问题。

---

## 摘要 (Abstract)

Feed-forward 3D Gaussian Splatting methods reconstruct a scene from posed or pose-free images in a single forward pass, yet current approaches predict one Gaussian per input pixel, tying the representation budget to camera resolution rather than scene complexity. A flat wall and a richly textured object thus produce equally many Gaussians despite very different geometric needs. We propose ZipSplat, a token-based feed-forward model that decouples Gaussian placement from the pixel grid. A multi-view backbone extracts dense visual tokens, and k-means clustering compresses them into a compact set of scene tokens. Cross- and self-attention refine these tokens, and a lightweight MLP decodes each into a group of Gaussians with unconstrained 3D positions. Because clustering is applied at inference, a single trained model spans the quality-efficiency curve without retraining. ZipSplat operates without ground-truth poses or intrinsics, yet sets a new state of the art on DL3DV and RealEstate10K with ${\sim}6{\times}$ fewer Gaussians than pixel-aligned methods, surpassing the best pose-free baseline by 2.1dB and 1.2dB PSNR, respectively. It further generalizes zero-shot to Mip-NeRF360 and ScanNet++, outperforming all comparable baselines. Our project page is at ${\href{https://veichta.com/zipsplat}{https://veichta.com/zipsplat}}$.

## AI 摘要

ZipSplat is a token-based feed-forward method that decouples 3D Gaussian placement from pixel grid, enabling efficient scene reconstruction with fewer Gaussians and superior performance on pose-free imaging tasks.

## 关键词

3D Gaussian Splatting, feed-forward, token-based model, multi-view backbone, k-means clustering, cross-attention, self-attention, MLP decoding, pose-free, DL3DV, RealEstate10K, Mip-NeRF360, ScanNet++
