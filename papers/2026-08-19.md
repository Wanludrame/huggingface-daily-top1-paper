# HuggingFace Daily Papers Top 1 - 2026-08-19

## StateM: Reaching 95.3% Raw Accuracy, or a \$15 Frontier Run, on Terminal-Bench 2.1 via Harness Scaling

- **arXiv ID**: 2608.15089
- **作者**: Ziheng Qin, Yaxin Lu, Zhangyang Atlas Wang, Kai Wang
- **提交者**: Kai Wang (@VictorKai1996NUS)
- **Upvotes**: 98
- **HuggingFace 链接**: https://huggingface.co/papers/2608.15089
- **arXiv 链接**: https://arxiv.org/abs/2608.15089

---

## 论文解读

### 一、核心贡献与创新点

- **提出 StateM 运行时系统**：不修改底层模型权重，而是通过“Harness Scaling”（执行框架扩展）提升长程 Agent 的可靠性。
- **将经验转化为可执行控制**：把复盘中的有效经验固化为持久状态、前置条件、操作规程与检查机制，避免 Agent 重复犯错。
- **强调状态化执行**：通过耐久状态、阶段局部上下文、受检状态转换、可恢复 Runbook 和版本化实践，解决任务执行中遗忘、跳步、状态漂移和过早终止的问题。
- **展示跨模型迁移能力**：论文报告其 Runbook 可在不同 GPT 系列模型间直接迁移，并可通过较低成本适配 DeepSeek 模型。
- **实现较高性价比**：摘要称，在 Terminal-Bench 2.1 上，StateM 可显著提升多种模型的成功率，同时将最终评分 API 成本降至约 \$15。

### 二、技术方法分析

StateM 的核心不是增强模型“思考能力”，而是约束和组织模型的**执行过程**：

1. **耐久状态（Durable States）**  
   将任务进展、关键决策、已验证结果和待处理事项显式保存，使 Agent 即使经历多轮工具调用或上下文变化，也能恢复正确工作状态。

2. **阶段局部上下文（Phase-local Context）**  
   按任务阶段提供相关信息，减少无关上下文干扰，并降低模型遗漏当前步骤要求的概率。

3. **受检转换（Checked Transitions）**  
   在从一个阶段进入下一阶段前检查前置条件。例如，只有确认文件存在、测试通过或命令返回符合预期时，才能进入后续步骤。

4. **可恢复 Runbook**  
   Runbook 类似面向 Agent 的操作手册：定义任务流程、异常处理、验证方式和恢复路径。执行失败后，Agent 可以依据既定规则回退、检查或重试，而非随意继续。

5. **版本化程序实践（Versioned Procedural Practices）**  
   将有效策略以可更新、可审查的版本形式保存，使用户与 Agent 能共同检查和迭代规则。这使“事后复盘”成为持续改进运行时控制的来源。

6. **机制匹配的规则设计**  
   论文强调规则并非普适提示词，而应针对共享执行结构的任务族设计。因而，在相似机制的任务上，具体规则具有更强泛化性。

整体上，StateM 相当于为 Agent 增加一个具备状态机、检查点、恢复机制和操作规范的执行层。

### 三、潜在影响与应用场景

- **终端与开发运维 Agent**：适用于代码修改、测试、调试、部署、环境配置和故障恢复等多步骤任务。
- **企业流程自动化**：可用于财务对账、工单处理、采购审批、数据录入等有明确阶段、前置条件和审计需求的工作流。
- **高可靠性工具调用**：对于需要多次调用 API、数据库、浏览器或命令行工具的 Agent，状态管理和受检转换可降低错误累积。
- **低成本模型增强**：若论文结果能在更多基准和真实场景复现，企业可借助运行时设计提升较低成本模型的能力，而不必完全依赖昂贵前沿模型。
- **可审计 Agent 系统**：版本化 Runbook 和显式状态有利于追踪 Agent 为何执行某一步，也便于人工审核、合规治理和问题定位。

其潜在限制是：Runbook 的设计、维护和任务族适配仍需要人工投入；对于开放式、目标不断变化或缺少明确验证条件的任务，规则化控制的收益可能较小。

### 四、推荐理由

- 论文关注了 Agent 落地中的关键瓶颈：模型会做单步任务，却常在长流程执行中失败。
- 方法不依赖重新训练模型，具备较好的工程可实施性与成本吸引力。
- “将复盘经验固化为运行时控制”的思路，具有较强的实践价值和可解释性。
- 摘要报告了跨模型、跨基准的实验结果，且特别强调成本效率，值得进一步阅读代码与完整实验设置验证。

一句话总结：StateM 通过状态机式执行控制和可恢复 Runbook，把 Agent 的经验转化为可检查、可复用的运行时机制，以提升长程任务的可靠性与性价比。

---

## 摘要 (Abstract)

Long-horizon agents can fail even when their underlying models can solve the constituent steps. They may lose track of mutable state, fail to reactivate lessons from earlier executions, skip known procedures, or stop prematurely. We bet on harness scaling to improve the execution system around an agent without changing its model weights. We introduce StateM, an agent-native runtime that organizes execution around durable states, phase-local context, checked transitions, recoverable runbooks, and versioned procedural practices that agents and users can inspect together.
  On Terminal-Bench 2.1, StateM raises GPT-5.5 xhigh to 92.1\%, versus 83.1\% reference and GPT-5.6 Sol Ultra at 91.9\%. The runbook transfers unchanged to GPT-5.6. With GPT-5.6 Sol xhigh, StateM reaches 95.3\% raw accuracy across 445 trials and succeeds on all 89 tasks at least once. The frozen profile raises GPT-5.6 Luna from 76.7 to 85.4\%, above the 84.9\% Sol xhigh reference.
  Using the same runtime, runbook structure, and golden rules, less than \$38 of adaptation raises DeepSeek-V4 Flash from 82.7 to 88.1\% under standard timeouts and to 89.1\% on an 88-task common core. Extending only the remaining latency-sensitive task matches the reported 88.8\% GPT-5.6 Sol max result. Final-score API usage is about \$15 versus \$574.68 for the GPT reference; total DeepSeek expenditure is \$52.22.
  On BusinessBench, family-specific runbooks built on development sets yield held-out gains of 0.55 macro and 1.34 micro points; two mechanism-matched families improve by 10.04 points. Concrete rules generalize when tasks share execution structure, while the control methodology applies broadly. StateM turns selected postmortem findings into persistent, executable preconditions and practices, making learned controls explicit and enforceable through stateful controls. Code at github.com/henryqin1997/statem.

## AI 摘要

StateM is a runtime system that improves long-horizon agent execution through durable states, recoverable runbooks, and enforceable procedural controls without altering model weights.

## 关键词

agent-native runtime, durable states, phase-local context, checked transitions, recoverable runbooks, versioned procedural practices, StateM, Terminal-Bench, runbook transfer, frozen profile, DeepSeek-V4 Flash, BusinessBench, mechanism-matched families, stateful controls
