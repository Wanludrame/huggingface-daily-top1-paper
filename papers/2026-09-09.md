# HuggingFace Daily Papers Top 1 - 2026-09-09

## Unlocking Lossless Speedups in LLMs via Discrete Diffusion

- **arXiv ID**: 2609.04010
- **作者**: Subham Sekhar Sahoo, Lingjie Chen, Khiem Pham, Jonathan Geuter, Chaitanya Dwivedi, Varad Pimpalkhute, Yash Akhauri, Alexander Moreno, Mikhail Yurochkin, Zhenting Wang, Mostafa Elhoushi, Nolan Dey, Shane Bergsma, Joel Hestness, John Thickstun, Eric Xing, Zhengzhong Liu
- **提交者**: Subham Sekhar Sahoo (@s-sahoo)
- **Upvotes**: 98
- **HuggingFace 链接**: https://huggingface.co/papers/2609.04010
- **arXiv 链接**: https://arxiv.org/abs/2609.04010

---

## 论文解读

### 一、核心贡献与创新点

- **提出扩散增强的自回归语言模型**：保留自回归模型的概率分布与 NTP 训练目标，同时利用扩散机制并行生成多个 token。
- **参数解耦**：将模型划分为：
  - **AR 权重**：负责标准的下一词预测；
  - **轻量级扩散权重**：负责并行采样与加速。
- **提出 Diffusion Distillation**：在现有 LLM 训练流程上增加较小开销，学习多 token 并行生成能力。
- **提出 $\Psi$-Spec 采样器**：无需额外 draft model，即可实现无损加速和固定上下文长度下的推理时扩展。
- **提出 Uno 模型**：既可以从头训练，也可以增强已有开源自回归模型。
- **实验效果突出**：摘要声称最高可实现约 $3\times$ 加速，并在多种 batch size 下超过主流 speculative decoding 方法。

### 二、技术方法分析

1. **以 AR 模型作为质量基础**  
   模型仍以 next-token prediction 为核心，因此理论上继承基础 AR 模型的语言建模能力，避免部分扩散语言模型常见的质量损失。

2. **通过扩散权重实现并行生成**  
   扩散模块尝试同时预测多个未来 token，而非严格按照单 token 顺序生成，从而减少串行解码步骤。

3. **扩散蒸馏训练**  
   通过蒸馏使扩散权重学习如何近似或复现 AR 模型的生成分布。这样可以在不大幅改变主模型参数的情况下，获得并行采样能力。

4. **$\Psi$-Spec 采样**  
   该方法类似于对并行候选结果进行验证与修正，但不依赖独立草稿模型。其核心价值在于：在保持目标 AR 分布的前提下，提高每次推理实际接受的 token 数量。

5. **与 speculative decoding 的区别**  
   speculative decoding 通常需要额外的 draft model；Uno 将相关能力整合到模型自身的轻量扩散参数中，可能降低部署复杂度和显存开销。

6. **方法局限**  
   “无损”主要指相对于基础 AR 分布或目标生成过程的保持，并不代表所有硬件、任务和 batch size 下都能获得相同加速。实际收益还会受到模型规模、序列长度、接受率、显存带宽和采样实现的影响。

### 三、潜在影响与应用场景

- **降低 LLM 推理延迟**：适用于聊天、代码补全、实时搜索和交互式代理。
- **提升高并发吞吐**：在批量推理和服务端部署中，可能减少单请求成本。
- **增强 Agent 工具调用**：工具调用通常包含多轮生成与较长上下文，加速可缩短整体任务执行时间。
- **适合代码与长上下文推理**：代码生成、文档分析和复杂规划任务往往生成 token 较多，潜在收益更明显。
- **利于开源模型升级**：如果能够稳定增强已有 AR 模型，开发者无需重新设计完整的扩散语言模型。
- **推动生成范式融合**：该工作尝试结合 AR 模型的质量稳定性与扩散模型的并行生成能力，可能影响未来推理架构设计。

### 四、推荐理由

- 同时关注**生成质量、推理速度和部署复杂度**，问题定义具有较强实际价值。
- 不依赖额外 draft model，可能比传统 speculative decoding 更易集成。
- 参数解耦设计有利于保护已有 AR 模型能力，并降低改造成本。
- 覆盖 agent、编码和长上下文推理等重要场景，应用前景较广。
- 不过，建议进一步核查完整论文中的速度测量标准、硬件配置、质量一致性验证及不同模型上的泛化结果。

总体而言，这篇论文提出了兼顾 AR 质量与扩散并行性的推理加速方案，值得关注其在真实生产环境中的稳定收益。

---

## 摘要 (Abstract)

Large Language Models (LLMs) owe much of their success to next-token prediction (NTP), but their autoregressive (AR) structure requires slow, sequential token generation. To overcome this bottleneck, we introduce diffusion-augmented LLMs, a new class of models that defines an AR model distribution while using diffusion to draw multiple tokens in parallel from that distribution. We decouple the parameters of these models into two sets: AR weights, trained using the standard NTP objective, and lightweight diffusion weights, trained to generate multiple tokens simultaneously. The diffusion weights are learned through a simple Diffusion Distillation phase that adds negligible overhead to existing LLM training pipelines. We also introduce $Ψ$-Spec, a family of samplers that enables lossless acceleration and inference-time scaling at a fixed context length. Unlike speculative decoding, our method requires no separate draft model. Unlike diffusion LLMs (d-LLMs), it accelerates generation without sacrificing the quality of the underlying AR model. The resulting models, called Uno, can be trained from scratch or built by augmenting existing open-weight AR LLMs. Uno achieves higher throughput than leading speculative-decoding methods at every evaluated batch size and delivers up to $3\times$ speedups over the base AR model, including at the largest batch size supported by the device. Notably, our 8B Uno model outperforms the leading open d-LLM, the 26B DiffusionGemma, and the proprietary Mercury 2 across all evaluated benchmarks in agentic tool use, coding, and long-context reasoning. We release code and checkpoints at: https://s-sahoo.github.io/uno/

## AI 摘要

Diffusion-augmented autoregressive language models use parallel token sampling via distilled diffusion weights and a specialized sampler to accelerate inference without quality loss or draft models.

## 关键词

next-token prediction, autoregressive, diffusion-augmented LLMs, diffusion distillation, Ψ-Spec, speculative decoding, diffusion LLMs, Uno
