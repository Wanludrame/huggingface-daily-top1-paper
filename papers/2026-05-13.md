# HuggingFace Daily Papers Top 1 - 2026-05-13

## Addressing Performance Saturation for LLM RL via Precise Entropy Curve Control

- **arXiv ID**: 2604.26326
- **作者**: Bolian Li, Yifan Wang, Yi Ding, Anamika Lochab, Ananth Grama, Ruqi Zhang
- **提交者**: Bolian Li (@lblaoke)
- **Upvotes**: 6
- **HuggingFace 链接**: https://huggingface.co/papers/2604.26326
- **arXiv 链接**: https://arxiv.org/abs/2604.26326

---

## 论文解读

### 一、核心贡献与创新点

本论文针对大语言模型（LLM）强化学习训练中普遍存在的**性能饱和**问题，提出了一种名为 **Entrocraft** 的新方法。其核心贡献与创新点包括：

1. **问题诊断精准化**：将 RL 训练性能饱和归因于**熵坍缩（entropy collapse）**，并指出现有正则化或裁剪方案无法保持长期熵曲线稳定。
2. **方法新颖性**：提出基于**拒绝采样（rejection sampling）**的简洁方案，通过对优势（advantage）分布进行偏置（biasing）来实现用户自定义的熵调度（entropy schedule）。
3. **理论贡献**：在最小假设下推导了**单步熵变化与优势分布之间的关系**，统一解释了已有 RL 方法及熵保持方法的行为。
4. **实证发现**：系统研究了熵调度策略，发现**线性退火（linear annealing）**——从高熵起始、缓慢衰减至略低目标——为最佳调度方案。
5. **无需修改目标函数**，且与优势估计器无关（advantage-estimator-agnostic），具备良好的通用性。

---

### 二、技术方法分析

- **核心机制**：Entrocraft 通过**拒绝采样**对训练样本的优势分布进行重塑，从而精确控制策略熵的演化轨迹，避免熵过早坍缩或剧烈波动。
- **理论框架**：建立了 per-step 熵变化与优势分布之间的解析关系，使得熵调度可被显式建模与控制。
- **调度设计**：通过对比多种熵调度方案（常数、指数、线性等），实证验证了**线性退火**的优越性，兼顾探索（高初始熵）与收敛（逐步降低熵）。
- **工程优势**：
  - 不引入额外正则项，避免与原 RL 目标冲突；
  - 对 PPO、GRPO 等不同优势估计算法均兼容；
  - 实现简单，易于嵌入现有 RLHF/RLVR 训练管线。
- **实验结果亮点**：
  - 4B 模型超越 8B 基线；
  - 训练有效时长延长 **4×**；
  - **pass@K 提升 50%**，显著增强输出多样性与泛化能力。

---

### 三、潜在影响与应用场景

- **训练效率提升**：可显著缓解 RL 后训练阶段的瓶颈，让中小规模模型在更长训练周期中持续获益，降低算力门槛。
- **推理能力增强**：对数学推理、代码生成、复杂规划等需要多样化探索的任务尤为有利（pass@K 提升直接受益）。
- **RLHF / RLVR 工业落地**：可作为通用插件嵌入到主流对齐与推理增强框架（如 GRPO、DAPO、VeRL 等）。
- **理论价值**：熵-优势关系的解析框架为后续 RL 算法设计提供了新的分析工具。
- **应用场景**：推理大模型（reasoning LLM）训练、代码大模型、Agent 决策训练、多样性生成需求强烈的开放域任务。

---

### 四、推荐理由

1. **问题真实且关键**：性能饱和是当前 LLM RL 训练（尤其是推理模型）最棘手的瓶颈之一。
2. **方法简洁优雅**：拒绝采样方案易实现、无侵入、可移植性强。
3. **理论与实证兼备**：既有解析推导，又有令人信服的实验结果（4B 超越 8B，pass@K +50%）。
4. **通用性强**：与具体优势估计算法解耦，适配性广。
5. **启发性高**：将"熵调度"从经验性技巧提升为可控、可设计的训练维度。

---

**一句话总结**：Entrocraft 通过拒绝采样精准操控熵曲线，以极简方式破解了 LLM 强化学习的性能饱和困局，兼具理论深度与工程实用价值。

---

## 摘要 (Abstract)

Reinforcement learning (RL) has enabled complex reasoning abilities in large language models (LLMs). However, most RL algorithms suffer from performance saturation, preventing continued gains as RL training scales. This problem can be characterized by the collapse of entropy, a key diagnostic for exploration in RL. Existing attempts focus on preventing entropy collapse through regularization or clipping. However, their resulting entropy curves often exhibit instability in the long term, which hinders performance gains. In this paper, we introduce Entrocraft, a simple rejection-sampling approach that realizes user-customized entropy schedule by biasing the advantage distributions. Entrocraft requires no objective regularization and is advantage-estimator-agnostic. Theoretically, we relate per-step entropy change to the advantage distribution under minimal assumptions. This explains the behavior of existing RL and entropy-preserving methods. Entrocraft also enables a systematic study of entropy schedules, which reveals that linear annealing, which starts high and decays to a slightly lower target, performs best. Empirically, Entrocraft addresses performance saturation, significantly improving generalization, output diversity, and long-term training. It enables a 4B model to outperform an 8B baseline, sustains improvement for up to 4x longer before plateauing, and raises pass@K by 50% over the baseline.

## AI 摘要

Entrocraft, a rejection-sampling approach for reinforcement learning, addresses performance saturation in large language models by customizing entropy schedules and improving generalization and training longevity.

## 关键词

reinforcement learning, entropy collapse, advantage distributions, entropy schedule, rejection-sampling, advantage-estimator-agnostic, entropy preservation, performance saturation, generalization, output diversity
