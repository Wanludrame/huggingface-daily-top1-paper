# HuggingFace Daily Papers Top 1 - 2026-04-17

## Target Policy Optimization

- **arXiv ID**: 2604.06159
- **作者**: Jean Kaddour
- **提交者**: Jean Kaddour (@JeanKaddour)
- **Upvotes**: 19
- **HuggingFace 链接**: https://huggingface.co/papers/2604.06159
- **arXiv 链接**: https://arxiv.org/abs/2604.06159

---

## 论文解读

### 一、核心贡献与创新点

TPO 将强化学习中的策略更新解耦为两步：**先构造目标分布，再拟合该分布**。传统方法（PG/PPO/GRPO）将"哪些补全应增加概率"和"参数如何移动"耦合在一起，容易因学习率、裁剪等超参数导致过冲或欠冲。TPO 通过显式构造目标分布 $q_i \propto p_i^{\text{old}} \exp(u_i)$，再用交叉熵损失拟合，梯度为 $p^\theta - q$，**当策略匹配目标时梯度自然消失**，提供了内置的稳定性。

### 二、技术方法分析

- **目标分布构造**：基于当前策略 $p^{\text{old}}$ 和评分信号 $u_i$，通过指数加权得到 $q_i$，本质上是对旧策略做一次封闭形式的"软最优"更新
- **策略拟合**：用交叉熵最小化 $H(q, p^\theta)$，等价于 KL 散度最小化，是一个标准的监督学习问题
- **梯度特性**：logit 上的梯度为 $p^\theta - q$，具有自平衡性质，无需额外裁剪机制
- 在 tabular bandits、transformer 序列任务和十亿参数级 LLM RLVR 上验证

### 三、潜在影响与应用场景

- **稀疏奖励场景**：TPO 显著优于现有方法，对数学推理、代码生成等奖励稀疏的 LLM 对齐任务价值大
- **简化调参**：解耦设计减少了对学习率、裁剪等超参的敏感性
- **通用性**：可作为 PPO/GRPO 的直接替代，适用于各类 RLHF/RLVR 流程

### 四、推荐理由

方法思路简洁优雅，将策略梯度中长期耦合的两个问题干净地分离，且在稀疏奖励这一实际痛点上表现突出。开源代码降低了复现门槛，对 LLM 对齐社区有直接实用价值。

> **一句话总结**：TPO 通过"先定目标分布、再交叉熵拟合"的解耦策略，为 LLM 强化学习提供了一种更稳定、在稀疏奖励下更优的替代方案。

---

## 摘要 (Abstract)

In RL, given a prompt, we sample a group of completions from a model and score them. Two questions follow: which completions should gain probability mass, and how should the parameters move to realize that change? Standard policy-gradient methods answer both at once, so the update can overshoot or undershoot depending on the learning rate, clipping, and other optimizer choices. We introduce \emph{Target Policy Optimization} (TPO), which separates the two questions. Given scored completions, TPO constructs a target distribution $q_i \propto p_i^{\,\mathrm{old}} \exp(u_i)$ and fits the policy to it by cross-entropy. The loss gradient on sampled-completion logits is $p^θ- q$, which vanishes once the policy matches the target. On tabular bandits, transformer sequence tasks, and billion-parameter LLM RLVR, TPO matches PG, PPO, GRPO, and DG on easy tasks and substantially outperforms them under sparse reward. Code is available at https://github.com/JeanKaddour/tpo.

## AI 摘要

Target Policy Optimization separates policy update decisions from probability assignment in reinforcement learning, improving performance over standard policy gradient methods in sparse reward scenarios.

## 关键词

policy-gradient methods, policy optimization, target distribution, cross-entropy, policy matching, tabular bandits, transformer sequence tasks, LLM RLVR, sparse reward
